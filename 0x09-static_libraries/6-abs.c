#include "main.h"

/**
 * _abs - Entry point
 * Description: Print absolute value
 * @n: Integer of absolute value
 * Return: int
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
